import { IScalar } from '../PluginManagers/ScalarPluginManager';
import { ValueNode } from 'graphql';
export declare class DateScalar implements IScalar<Date, string> {
    serialize(value: Date): string;
    parseValue(value: string): Date | void;
    parseLiteral(valueNode: ValueNode, variables?: {
        [p: string]: any;
    }): Date | void;
}

import { IScalar } from '../PluginManagers/ScalarPluginManager';
import { ValueNode } from 'graphql';
export declare class RegExpScalar implements IScalar<RegExp, string> {
    serialize(value: RegExp): string;
    parseValue(value: string): RegExp;
    parseLiteral(valueNode: ValueNode, variables?: {
        [p: string]: any;
    }): RegExp | void;
}
